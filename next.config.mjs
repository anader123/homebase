/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "basepaint.xyz",
      "nouns.build",
      "cdn.mint.fun",
      "img.reservoir.tools",
      "ik.imagekit.io",
    ],
  },
};

export default nextConfig;
