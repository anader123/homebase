/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "basepaint.xyz",
      "nouns.build",
      "cdn.mint.fun",
      "img.reservoir.tools",
    ],
  },
};

export default nextConfig;
